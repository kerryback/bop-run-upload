# REPORT.md has moved to `archive/REPORT.md`

It is the legacy narrative of the pre-protocol study: 24 economies, 21 of which have no code in this
repository and never did, every figure a single seed, and the KP14 and GS21 levels measured on
calibrations since corrected. **No live document may cite it.** See `docs/RESULTS.md` for live results
and its "Routes with no code" appendix for what the current code can and cannot express.

This stub exists so that existing references keep resolving. Two of them are comments in
`variants/gs_bx/gs_solve_gam.py`, which is a SOLVE PRODUCER: manifest `8b584c38614695ac` records it
under `sources`, so editing that file -- even a comment -- re-keys g28's solve and gx7's four, about
25 h of re-solving for nothing (`variants/solve_impact.py` exists to catch exactly this).
