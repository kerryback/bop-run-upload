# Core numerical computing
numpy>=1.20.0
pandas>=1.3.0
scipy>=1.7.0

# Statistical analysis
statsmodels>=0.13.0
scikit-learn>=1.0.0

# Parallel processing
joblib>=1.1.0

# pandas I/O backends -- never imported, resolved at runtime inside pandas, so an
# import scan does not find them. Missing pyarrow broke --save_panel on Sol 2026-09-07.
pyarrow>=14.0.0
openpyxl>=3.1.0

# AWS S3 integration for result storage
boto3>=1.26.0
requests>=2.31.0
